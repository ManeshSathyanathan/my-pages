name: Demo Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 22f3000236@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Show repo files
        run: ls -la